# Команда проекта

## Распределение ответственности

### 👤 Даня - Data Collection & Generation
**Папка:** `даня/`

**Задачи:**
- Сбор реальных исторических фотографий WWII (~800 изображений)
- Генерация синтетических изображений с помощью SDXL-Turbo (200 изображений)
- Создание манифестов данных
- Контроль качества датасета

**Файлы:**
- `data/download_real_wikimedia.py`
- `data/generate_synthetic_sdxl.py`
- `data/real/` - реальные изображения
- `data/synth/` - синтетические изображения

---

### 👤 Боря - Data Processing & Embeddings
**Папка:** `боря/`

**Задачи:**
- Объединение манифестов реальных и синтетических данных
- Train/Val/Test split (80/10/10)
- Извлечение мультимодальных CLIP эмбеддингов (image + text)
- Сохранение эмбеддингов для обучения

**Файлы:**
- `src/build_manifest.py`
- `src/extract_embeddings.py`
- `data/manifest.csv`
- `data/embeddings.npz`

---

### 👤 Филипп - Model Training & Evaluation
**Папка:** `филипп/`

**Задачи:**
- Обучение логистической регрессии на CLIP эмбеддингах
- Оценка метрик (accuracy, precision, recall, F1)
- Построение confusion matrix
- Реализация инференса на новых данных
- Error analysis

**Файлы:**
- `src/train_classifier.py`
- `src/predict.py`
- `models/clip_fake_detector.pkl`
- `models/metrics.json`

---

### 👤 Илья - Web Application & Deployment
**Папка:** `илья/`

**Задачи:**
- Разработка Flask API для инференса
- Создание веб-интерфейса с drag & drop
- Визуализация результатов детекции
- Винтажный дизайн в стиле секретных документов 1940-х
- Тестирование веб-приложения

**Файлы:**
- `webapp/app.py`
- `webapp/templates/index.html`
- `webapp/static/css/style.css`
- `webapp/static/js/main.js`

---

## Порядок работы

1. **Даня:** Собирает и генерирует данные → создает манифесты
2. **Боря:** Объединяет данные → извлекает CLIP эмбеддинги
3. **Филипп:** Обучает модель → оценивает метрики → реализует инференс
4. **Илья:** Создает веб-приложение → интегрирует модель → деплоит

## Git workflow

Каждый участник работает в своей папке и коммитит изменения:

```bash
# Пример коммита для Дани
git add даня/
git commit -m "Add real WWII images from Wikimedia Commons"

# Пример коммита для Бори
git add боря/
git commit -m "Extract CLIP embeddings for all images"

# Пример коммита для Филиппа
git add филипп/
git commit -m "Train logistic regression classifier"

# Пример коммита для Ильи
git add илья/
git commit -m "Create Flask API for model inference"
```

## Общие файлы

Если нужно изменить общие файлы (не в личных папках), согласуйте изменения с командой:
- `requirements.txt` - общие зависимости
- `README.md` - главная документация
- `.gitattributes` - Git LFS конфигурация
